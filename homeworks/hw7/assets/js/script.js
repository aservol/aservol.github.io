function averageThreeNumbers(x, y, z) {
    let sum = (x + y + z);
    let x = getRandomNum (50);
    let y = getRandomNum (50);
    let z = getRandomNum (50);
    return avg = sum / 3;
}

function ThreeVariables (x, y, z) {
    let x = getRandomNum (20);
    let y = getRandomNum (10);
    let z = getRandomNum (13);
}


function createSentence (num, noun) {
    let num = 12;
    let noun = "squirrel";
    let result = "On average, a Berkeley student has " + num + noun ;
    return result;
}

function getRandomNum (max) {
return Math.floor(Math.random() * max);
}
console.log(Math.random());

